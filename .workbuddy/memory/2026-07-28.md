# 2026-07-28 工作日志

## 任务：L5 T+0 日内做T策略 — 代码与策略深度优化分析（只读）

对 `D:\project\a-t0-v2` 量化交易系统做静态审查，产出优化点 + 实验步骤，写入 `docs/OPTIMIZATION_ANALYSIS.md`。所有发现经 grep 二次验证（未改代码）。

### 已验证的关键发现（源码实证）
- **死代码**：`open_max_vwap_dev`（strategy.py:108 仅定义，全 src 仅此一处，evaluate 从不读取）；`min_net_expected_return`（src 零引用）；`_compute_pairing_threshold` + `open_vwap_dev` 字段（@deprecated 均值回归遗留）。
- **配置加载缺口**：`load_backtest_params`（config.py:106）只读 `backtest` 段，不读 `cost` → pessimistic/optimistic 成本场景在回测中不生效；`load_cost_model` 仅 cli.py 调用。
- **overlay 失效**：`paper.yaml` 用 `max_position_pct/max_t_size/eod_force_flat`，但 `RiskParams` 字段为 `max_t_size_ratio/...`，`hasattr` 过滤静默丢弃 → 模拟盘 15% 上限/尾盘强平不生效。
- **回测/实盘不一致**：`backtest_single_day`（backtest.py:666/677）直调 `evaluate_reduce/add_signal`，绕过 `evaluate_all_signals` 内的 `market_gate_for_add` 市场门控 → 回测未反映情绪风控。
- **dataclass vs yaml 分歧**：`BacktestParams` 默认 `stop_loss=0.008/cooldown=12/trailing=0.005`，yaml 为 `0.02/3/0.0`（入口已覆盖 yaml，但 PyYAML 缺失时回退错值）。
- **数据泄漏**：`backtest_zz500.py:608` 振幅筛选用 `in_range_dates[:window]`（回测窗口内前 60 日）选股 → 样本内选择偏差，F5 "样本外"性质打折。
- **寻优网格含死参数**：`PARAM_GRID` 含 `vwap_dev_atr_multiplier/rsi_*`（活跃信号不用），结果对当前策略意义有限（line 1604 已钉死 vwap_dev_atr_multiplier=[0.8]）。

### 优势（保持不动）
- `features.py` 指标层严格因果无前视偏差；`execution.py` FIFO 配对/T+1 锁定/文件锁正确；振幅筛选 Youden 阈值(0.0494)有统计依据。

### 待用户拍板
- 实验5：清理 `open_max_vwap_dev`(A) vs 重新启用(B)？
- 实验1：本地是否存 start_date 之前更早行情？（决定泄漏能否彻底消除）
- paper.yaml 字段：改名对齐 RiskParams 还是 RiskParams 新增字段？

### 后续修改代码时务必注意
1. 改 `evaluate_*` 信号函数后要同步 `evaluate_all_signals` 与回测路径，避免再次出现回测/实盘分叉。
2. 任何新增 yaml 字段必须在对应 dataclass 存在且 `load_*` 用 `hasattr` 白名单放行，否则静默失效。
3. 振幅筛选改动需配套消除/标注数据泄漏。
