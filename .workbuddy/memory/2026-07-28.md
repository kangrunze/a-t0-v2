# 2026-07-28 工作日志

## 项目逻辑梳理（用户请求：@scene#28 金融服务）
- 任务：梳理 `D:\project\a-t0-v2` 全部代码+文档，整理整体逻辑、买卖点条件与参数。
- 完成度：已读取 README / docs/TECHNICAL_GUIDE.md / config/thresholds.yaml + config.py / src/at0 全部核心模块（strategy/risk/features/execution/screener/backtest/cli；backtest.py 已全文读完 1727 行）。
- 产出：HTML 报告 `outputs/project_logic_report.html`（架构图 + 买卖点条件清单 + 6 张参数对照表）。
- 核心结论：
  - 趋势跟随型 L5 T+0（2026-07-24 由均值回归转向，因 5min VWAP 偏离 65% 延续/29% 回归）。
  - 开仓三层 confluence（极值≥2 + 确认≥1 + 环境通过）：reduce=VWAP_dev<0/ADX>35/KDJ死叉 + 量比>2 + 未涨停；add=VWAP_dev>0/ADX>35/KDJ金叉 + 量比>2 + 板块未退潮且未跌停。
  - 平仓=趋势反转(VWAP穿越/ADX<28/偏离转反向) + 价格方向确认(2根≥60%同向)，不走分层总分。
  - 止损安全网：trailing(0.4%激活/0.5回撤) + 固定止损 0.4% + 超时 expired。
  - 参数集中 config/thresholds.yaml，overlay(research/paper) + dataclass 兜底。
- 注意：regime 过滤已废弃(backtest 中 regime_filter_enabled 保留但不应启用)；样本外主依据 min_amplitude_long=0.0494。

## 用户追问：买卖点条件 / 振幅 细节澄清
- 振幅不属于三层信号引擎，是更上游 screener 层(选股/样本筛选)的前置门槛：不过振幅筛选的股票不进入 evaluate_reduce/add_signal。
  - 20日振幅 ≥ 3.5% (min_20d_amplitude, 始终生效)；公式 (high-low)/prev_close 取均值。
  - 60日日均振幅 ≥ 4.94% (min_amplitude_long, Youden最优)；代码默认 None，但 config/thresholds.yaml:106 已显式设 0.0494 经 overlay 加载生效（开启时拉60根日K）。
  - screener 第4项 min_capture_spread=0.006 用当日 (high-low)/prev_close 近似预期捕获空间（日内振幅，日级检查，非逐K线信号条件）。
- 振幅=入场券(能不能做)，三层信号=触发器(何时做)，风控min_capture_spread=0.72%=落单门槛(这笔够不够)。
- 校正：open_max_vwap_dev=0.012 在 SignalParams/thresholds.yaml/params.json 均定义并导出，但 strategy.py 与 backtest.py 均未引用 → 是"已声明未接线"的潜参数，并未真正拦截深偏离开仓；真正的波动空间保护是 screener 振幅筛选。
- 产出（用户要求"重新生成完整说明"）：`outputs/opening_conditions_full.html`——代码(strategy/features/risk/screener 行号)+thresholds.yaml数值+指标算法(VWAP偏离/ADX/KDJ/量比公式)三者结合的完整文档，含减仓/加仓逐项条件表、触发判定、市场层权重、风控附加门槛、振幅筛选、平仓条件、open_max_vwap_dev未接线校正。

## F5 完整 A/B 对比结果（500股全池 · 3年期）— 已确认锁定 min_amplitude_long=0.0494

- baseline 全部(500): 净亏 -421,766，盈利占比 22.4%，成本/毛利 240.3%。
- amp_on 全部通过(36): 净盈 +151,364，盈利占比 69.4%，成本/毛利 42.5%。
- A/B 翻正 +573,130（近57万）。样本外证据最强：27 只独立股净盈 +128,622（占 amp_on 总盈亏 85%），盈利占比 70.4%，成本/毛利 42.0%，4/4 标准全通过。
- 无过拟合：样本外均值 +4,764 > 训练集 +2,527；可交易池 36 只（27 只独立），解决原担心的 9 只集中度风险。
- 误伤率 18.5%：样本外筛掉 373 只中 69 盈利 / 304 亏损，被筛掉合计 -475,887（亏损主力）；筛除逻辑成立。
- 结论：F5 样本外证据完全支持 min_amplitude_long: 0.0494 作为默认配置。config/thresholds.yaml:106 已设该值且经 F4/StageD 前置引用；今日追加 [2026-07-28 已确认] 注释。
- 结构化结果：`outputs/backtest/stepF5_oos_500.json`（含 baseline_overall / baseline_oos + amp_on 全/训练/样本外）。
- ⚠️ 数据缺口：chat 报告中的 baseline 训练集(100) -74,500 / 24.0% / 235.0% 未写入该 JSON（仅存对话），如需完整复现应补入。
- 下一步候选：(a) 将 baseline 训练集100 数值补入 JSON；(b) research/paper overlay 下跑全池复核；(c) 进入下一研究问题 F6。
